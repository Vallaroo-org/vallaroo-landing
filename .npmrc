# NPM Configuration for AWS Amplify
# This file helps resolve peer dependency conflicts

# Use legacy peer deps resolution
legacy-peer-deps=true

# Set registry
registry=https://registry.npmjs.org/

# Enable strict SSL
strict-ssl=true

# Set cache directory
cache=~/.npm

# Set log level
loglevel=warn
